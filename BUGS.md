- The GH Actions script responsible for syncing the ML notebooks does not run on command (as it should) but rather auto runs every 24 hours. I want it to run on command. The script also grabs output content from each code block, which it should ignore.

- Within blogs, clicking on elements with the `details` tag, triggers their state to open, but this does not render the internal `p` tagged paragraphs.

